# Unit 6 archived lesson sequences

The HTML files directly in this folder are the older arrays/parallel-lists Unit 6. `files-databases-sequence/` preserves the files, CSV, dependencies, SQL, and software-maintenance unit used immediately before the Python Project Studio rebuild. Archived pages and code are retained for instructor reference and excluded from active navigation.

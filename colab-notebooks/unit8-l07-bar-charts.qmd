---
title: '**Dogs Dataset**'
jupyter: python3
---

**IMPORTANT!!** You must run each code block from top to bottom. First, run the import block, then the "Create a dataframe" block, then the "df.columns" block, etc. The code will not execute until you run the block by clicking on the play button. To see the play button, hover over the [ ] in each block.

```{python}
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

sns.set_theme(style="whitegrid")
```

The data loads straight from the web - just run the cell below, there's nothing to download.

```{python}
# Create a dataframe from our dogs.csv file
df = pd.read_csv("https://drive.google.com/uc?export=download&id=1JB7i-u7q3YpuxihimQG5GiGTKac-_MLj")
```

```{python}
df.columns
```

# Bar Chart

A bar chart counts how many times each value appears in a column. Seaborn's `countplot` does the counting *and* the drawing in one step - we just hand it the column. (We use `order=` to put the bars in order along the x-axis.)

```{python}
fig, ax = plt.subplots(figsize=(14, 8))

# count how many breeds have each 'Max Life Span' value
sns.countplot(data=df, x="Max Life Span",
              order=sorted(df["Max Life Span"].dropna().unique()), ax=ax)

# Add labels to the chart
ax.set(xlabel="# Years", ylabel="Count", title="Count of Max Life Spans")
```
